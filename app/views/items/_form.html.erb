<%= bootstrap_form_for(@item) do |f| %>

    <%= f.alert_message 'The Item could not be saved due to the following errors:' %>

    <%= render partial: 'shared/item_form_fields', locals: { f: f } %>

    <%= f.primary 'Save' %>

<% end %>