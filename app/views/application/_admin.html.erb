<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Manage <span class="caret"></span></a>
<ul class="dropdown-menu">
  <%= content_tag :li, link_to('Repositories', repositories_path) if can? :index, Repository %>
  <%= content_tag :li, link_to('Collections', collections_path) if can? :index, Collection%>
  <%= content_tag :li, link_to('Items', items_path) if can? :index, Item %>
  <%= content_tag :li, link_to('Batches', batches_path) if can? :index, Batch %>
  <%= content_tag :li, link_to('Roles', roles_path) if can? :index, Role %>
  <%= content_tag :li, link_to('Users', users_path) if can? :index, current_user %>
  <%= content_tag :li, link_to('Subjects', subjects_path) if can? :index, Subject %>
</ul>
