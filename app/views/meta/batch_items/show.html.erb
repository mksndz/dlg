<%= content_for :admin_area_title do %>
    Show Item
<% end %>

<%= content_for :admin_area_subtitle do %>
    <%= @batch_item.dc_title.first %>
<% end %>

<%= link_to 'Edit', edit_batch_batch_item_path(@batch, @batch_item), class: 'btn btn-default' %>

<br><br>

<%= render('shared/show_field', field_name: t('activerecord.attributes.batchitem.collection_id'), value: @batch_item.collection.display_title) if @batch_item.collection %>
<%= render 'shared/show_field', field_name: t('activerecord.attributes.batchitem.slug'), value: @batch_item.slug %>
<%= render 'shared/show_field', field_name: t('activerecord.attributes.batchitem.dpla'), value: @batch_item.dpla %>
<%= render 'shared/show_field', field_name: t('activerecord.attributes.batchitem.public'), value: @batch_item.public%>

<%= render partial: 'shared/show_dc_fields', locals: { entity: @batch_item} %>

<%= render 'shared/show_field', field_name: t('activerecord.attributes.batchitem.created_at'), value: @batch_item.created_at %>
<%= render 'shared/show_field', field_name: t('activerecord.attributes.batchitem.updated_at'), value: @batch_item.updated_at %>
