<%= content_for :admin_area_title do %>
    Repositories
<% end %>

<%= content_for :admin_area_subtitle do %>
    All of them
<% end %>

<%= link_to 'Add Repository', new_meta_repository_path, class: 'btn btn-default' %>

<br><br>

<% if @repositories.empty? %>

    <p>No repositories have been added yet.</p>

<% else %>

    <table class="table table-condensed table-hover">
      <thead>
      <tr>
        <th>
          <%= sortable 'id', 'ID' %>
        </th>
        <th>
          <%= sortable 'slug', 'Slug' %>
        </th>
        <th>
          <%= sortable 'title', 'Title' %>
        </th>
        <th>
          Collections
        </th>
        <th width="185px"></th>
      </tr>
      </thead>

      <tbody>
      <% @repositories.each do |repository| %>
          <tr>
            <td>
              <%= link_to repository.id, meta_repository_path(repository) %>
            </td>
            <td>
              <%= link_to repository.slug, meta_repository_path(repository) %>
            </td>
            <td>
              <%= repository.title %>
            </td>
            <td>
              <%= repository.collections.count %>
            </td>
            <td>
              <div class="btn-group btn-group-xs">
                <%= render partial: 'shared/entity_actions', locals: { base_path: meta_repository_path(repository), edit_path: edit_meta_repository_path(repository), entity: repository, show_bl: false } %>
              </div>
            </td>
          </tr>
      <% end %>
      </tbody>
    </table>

    <%= paginate @repositories %>

<% end %>