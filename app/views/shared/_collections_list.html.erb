<div class="well">
  <p><strong>Collections</strong></p>
  <ul class="list-unstyled">
    <% @collections.each do |c| %>
        <li><%= link_to c.display_title, items_for_path(c) %></li>
    <% end %>
  </ul>
</div>