<div class="navbar-right">
  <ul class="nav navbar-nav">
    <%= render_nav_actions do |config, action|%>
      <li><%= action %></li>
    <% end %>
    <% if current_user %>
      <li>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user %> <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <%= content_tag :li, link_to(t('meta.app.user_menu.profile'), profile_path) %>
          <%= content_tag :li, link_to(t('meta.app.user_menu.change_password'), edit_profile_path) %>
          <%= content_tag :li, link_to(t('meta.app.user_menu.saved_searches'), blacklight.saved_searches_path) %>
          <%= content_tag :li, link_to(t('meta.app.user_menu.search_history'), blacklight.search_history_path) %>
          <%= content_tag :li, link_to(t('meta.app.user_menu.bookmarks'), bookmarks_path) %>
          <%= content_tag :li, link_to(t('blacklight.header_links.logout'), destroy_user_session_path, method: :delete) %>
        </ul>
      </li>
    <% end %>
  </ul>
</div>
