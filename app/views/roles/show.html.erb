<%= content_for :title do %>
    Show Role
<% end %>

<%= content_for :subtitle do %>
    <%= @role.name %>
<% end %>

<%= link_to 'Edit', edit_role_path(@role), class: 'btn btn-default' %>

<br><br>

<%= render 'shared/show_field', field_name: 'Name', value: @role.name %>