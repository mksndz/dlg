<%= bootstrap_form_for(@batch) do |f| %>

    <%= f.alert_message t('meta.defaults.messages.errors.invalid_on_save', entity: 'Batch') %>

    <%= f.text_field :name %>

    <%= f.text_area :notes %>

    <%= f.primary t('meta.defaults.actions.save') %>

<% end %>