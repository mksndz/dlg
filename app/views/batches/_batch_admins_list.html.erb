<div class="well">
  <p><strong>Users</strong></p>
  <ul class="list-unstyled">
    <% @users.each do |u| %>
        <li><%= link_to u.email, filtered_batches_path(u) %></li>
    <% end %>
  </ul>
</div>