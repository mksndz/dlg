<%= content_for :title do %>
    Edit Batch
<% end %>

<%= content_for :subtitle do %>
    <%= @batch.name %> by <%= @batch.user.email %>
<% end %>

<% breadcrumb :batch, @batch %>

<%= render 'form' %>