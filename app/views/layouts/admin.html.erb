<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">

    <!-- Mobile viewport optimization h5bp.com/ad -->
    <meta name="HandheldFriendly" content="True">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    
    <!-- Internet Explorer use the highest version available -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <!-- Mobile IE allows us to activate ClearType technology for smoothing fonts for easy reading -->
    <!--[if IEMobile]>
      <meta http-equiv="cleartype" content="on">
    <![endif]-->

    <title><%= render_page_title %></title>
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag "admin_application", media: "all" %>
    <%= javascript_include_tag "admin_application" %>
    <%= csrf_meta_tags %>
    <%= content_for(:head) %>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>

  <body class="">
    <a name="top"></a>

    <% if current_admin %>
      <%= render partial: 'meta/shared/header_navbar' %>
    <% end %>

    <div class="container-fluid">
      <%= content_tag :h1, application_name, class: 'sr-only application-heading' %>

      <%= render partial: '/flash_msg', layout: 'shared/flash_messages' %>

      <div class="row-fluid">
        <div class="page-header">
          <h1>
            <%= yield :admin_area_title %><small><%= yield :admin_area_subtitle %></small>
          </h1>
        </div>
        <div class="col-md-9">
          <%= yield %>
        </div>
        <div class="col-md-3">
          <%= yield :sub_menu %>
        </div>
      </div>

    </div>

    <%= render partial: 'meta/shared/footer' %>
  </body>

</html>
