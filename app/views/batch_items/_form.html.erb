<%= bootstrap_form_for([@batch, @batch_item]) do |f| %>

    <%= f.alert_message t('meta.defaults.messages.errors.invalid_on_save', entity: 'Batch Item') %>

    <%= f.text_field :slug %>

    <%= f.select :collection_id, @collections.collect { |c| [c.display_title, c.id] }, { include_blank: true } %>

    <%= f.form_group :public do %>
        <%= f.check_box :public %>
    <% end %>

    <%= f.form_group :dpla do %>
        <%= f.check_box :dpla %>
    <% end %>

    <%= f.select :other_collections, @collections.collect { |c| [c.display_title, c.slug] }, { include_blank: true }, { multiple: true,  class: 'fancy-multiselect' } %>

    <%= render partial: 'shared/edit_dc_fields', locals: { f: f, entity: @batch_item } %>

    <%= f.primary t('meta.defaults.actions.save') %>

<% end %>